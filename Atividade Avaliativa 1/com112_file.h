#include <stdio.h>
#include <stdlib.h>

void readFile(int *array, int length);
void writeFile(int length, int *array, int flag);
void writeReport(int length, int comparison, int movement, int flag, int secondFlag, int time);