#include <stdio.h>
#include <stdlib.h>

void writeFile(int length, int *array, int flag);
void reportData(FILE *fp, double time, int comparion, int moved);
void writeReport(int length, int time, int comparison, int movement, int flag);